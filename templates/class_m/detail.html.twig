{% extends 'base1.html.twig' %}

{% block body %}
	<div class="container col-md-6 mt-3" style="font-family:Comic Sans MS">
		<h3 class="text-success text-center text-capitalize">CLASS DETAIL</h3>
		<a class="btn btn-outline-success mt-2" href="{{ path('classM_index') }}">Classes List</a>
		<table class="table table-striped text-center mt-2">
			<thead>
				<tr>
					<th>ID</th>
					<th>Name</th>
					<th>Floor</th>
					<th>Lecturer</th>					
                	<th>Student</th>
					<th>Student count</th>
					<th>Lecturer count</th>
					<th>Menu</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td>{{ class.id }}</td>
					<td>{{ class.name}}</td>
					<td>{{ class.floor }}</td>
					<td>
                	{% for c in class.lecturer %}
						<a href="{{ path("lecturer_detail",{'id' : c.id}) }}" class="">
                            {{c.name}}
                            <br>
                        </a>            	
						<br/>
                	{% endfor %}
                	</td>
					<td>
						{% for s in class.student %}
                        <a href="{{ path("student_detail",{'id' : s.id}) }}" class="">
                            {{ s.name }}
                            <br>
                        </a>
                    {% endfor %}
					</td>
					<td>{{ count }}</td>
					<td>{{ countn }}</td>
					<td>
						
						<a class="btn btn-outline-dark" href="{{ path('classM_edit', {'id': class.id}) }}">EDIT</a>
						
						<a class="btn btn-outline-dark" href="{{ path('classM_delete', {'id': class.id}) }}"
                  onclick= "return confirm('Are you sure to delete this student ?')"
                  >DELETE</a>
				  		
					</td>
				</tr>
			</tbody>
		</table>

	
	</div>
{% endblock %}
